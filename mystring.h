#ifndef MY_STRING
#define MY_STRING

const char* strchr(const char*, const char);

int strcmp(const char*, const char*);

int strncmp(const char*, const char*, size_t);

char* strcpy(char*, const char*);

char* strncpy(char*, const char*, size_t);

char* strtok(char*, char*);




#endif MY_STRING
